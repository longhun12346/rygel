diff --git a/vendor/pdfio/pdfio-content.c b/vendor/pdfio/pdfio-content.c
index 7221401c4a..ee49abb3cb 100644
--- a/vendor/pdfio/pdfio-content.c
+++ b/vendor/pdfio/pdfio-content.c
@@ -2840,6 +2840,7 @@ copy_png(pdfio_dict_t *dict,		// I - Dictionary
     pdfioDictSetArray(dict, "ColorSpace", pdfioArrayCreateColorFromStandard(dict->pdf, num_colors, PDFIO_CS_SRGB));
   }
 
+#if 0
   if (png_get_tRNS(pp, info, /*trans_alpha*/NULL, &num_trans, &trans))
   {
     int			m;		// Looping var
@@ -2881,6 +2882,7 @@ copy_png(pdfio_dict_t *dict,		// I - Dictionary
 
     pdfioDictSetArray(dict, "Mask", mask);
   }
+#endif
 
   // Create the image object...
   obj = create_image(dict, pixels, width, height, depth, num_colors, alpha);
